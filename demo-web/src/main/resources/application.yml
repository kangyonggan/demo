spring:
  profiles:
    active: @env@
  jackson:
    serialization:
      write-dates-as-timestamps: true
  redis:
    host: localhost
    port: 6379
    password: 123456

mybatis:
  mapper-locations: classpath*:mapper/*Mapper.xml